# vars file
---
shinycannon_download_url: "{{ shinycannon_download_url_without_extension }}shinycannon_{{ shinycannon_version }}_{{ shinycannon_machine_map[ansible_machine] }}.deb"

shinycannon_install:
  - default-jdk
  - default-jre
